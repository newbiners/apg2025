const Title = ({ title }) => {
  return (
    <div>
      <h1 className="text-center sm:text-6xl font-bold mb-11 sm:mb-28 text-xl">
        {title}
      </h1>
    </div>
  );
};

export default Title;
